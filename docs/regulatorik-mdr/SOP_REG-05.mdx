---
id: SOP_REG-05
title: Umgang mit Benannten Stellen & Behörden
sidebar_label: Audits & Inspektionen
---

import React, { useState, useEffect } from 'react';
import SopQuiz from '@site/src/components/SopQuiz';

export const quizData = [
  { id:1, question:"Was ist das Ziel der SOP-REG-05?", options:["Minimierung der Behördenkontakte durch interne Lösungen","Transparente, gesetzeskonforme und zeitnahe Berichterstattung sowie Vorbereitung auf angekündigte und unangekündigte Inspektionen","Vermeidung jeglicher Behördenkommunikation","Optimierung der Zertifizierungskosten"], correct:1, explanation:"SOP-REG-05 zielt auf transparente, gesetzeskonforme und zeitnahe Berichterstattung gemäß MDR und bereitet die EMIG GmbH auf unangekündigte oder angekündigte Inspektionen vor." },
  { id:2, question:"Wer ist die zentrale Anlaufstelle für alle Behördenkommunikation?", options:["Der Lagerleiter als erste Kontaktperson","Die Geschäftsführung für alle Anfragen","Die PRRC — alle Kommunikation mit Überwachungsbehörden wird über die PRRC zentralisiert","Der QMB für operationelle Anfragen"], correct:2, explanation:"Sämtliche Kommunikation mit Überwachungsbehörden wird über die PRRC zentralisiert. Dies verhindert widersprüchliche Aussagen und stellt sicher, dass alle Antworten regulatorisch korrekt sind." },
  { id:3, question:"Über welche Kanäle erfolgt die Meldung schwerwiegender Vorkommnisse?", options:["Per Telefon an das BfArM direkt","Elektronisch über die entsprechenden Portale (EUDAMED/BfArM) innerhalb der gesetzlichen Fristen","Per Einschreiben an alle zuständigen Behörden","Nur intern — externe Meldung ist optional"], correct:1, explanation:"Meldungen schwerwiegender Vorkommnisse erfolgen elektronisch über die Portale EUDAMED und/oder BfArM — innerhalb der gesetzlichen Fristen. Die PRRC ist für die fristgerechte Einreichung verantwortlich." },
  { id:4, question:"Was muss EMIG bei amtlichen schriftlichen Anfragen von Behörden tun?", options:["Nur relevante Teile beantworten","Vollständig innerhalb der gesetzten Fristen antworten und alle geforderten Unterlagen bereitstellen","Anfragen zunächst durch einen Anwalt prüfen lassen bevor geantwortet wird","Anfragen nach Möglichkeit ignorieren bis eine zweite Anfrage kommt"], correct:1, explanation:"Amtliche Anfragen sind innerhalb der gesetzten Fristen vollständig zu beantworten. Die PRRC stellt sicher, dass alle geforderten Proben oder Unterlagen (z.B. technische Dokumentationen des Herstellers) zur Verfügung gestellt werden." },
  { id:5, question:"Was ist eine Benannte Stelle?", options:["Eine staatlich benannte Organisation, die die Konformitätsbewertung von Medizinprodukten prüft","Ein internes EMIG-Prüfgremium für Qualitätsfragen","Der technische Dienst des BfArM","Ein externes Beratungsunternehmen für Regulatorik"], correct:0, explanation:"Eine Benannte Stelle (Notified Body) ist eine staatlich benannte Organisation, die die Konformitätsbewertung von Medizinprodukten prüft. Sie stellt CE-Zertifikate für Produkte der Klassen IIa, IIb und III aus." },
  { id:6, question:"Was prüft die PRRC regelmäßig in Bezug auf Benannte Stellen?", options:["Die Gebührenstruktur der Benannten Stellen","Die Gültigkeit der von den Benannten Stellen ausgestellten CE-Zertifikate der Lieferanten","Nur ob die Benannte Stelle selbst zertifiziert ist","Die Anzahl der von Benannten Stellen ausgestellten Zertifikate weltweit"], correct:1, explanation:"Die PRRC prüft regelmäßig die Gültigkeit der CE-Zertifikate, die von den Benannten Stellen für die Hersteller der EMIG-Lieferanten ausgestellt wurden. Abgelaufene CE-Zertifikate machen Produkte nicht mehr vermarktungsfähig." },
  { id:7, question:"Welches Protokoll gilt bei einer unangekündigten Inspektion am Standort Reutlingen?", options:["Inspektor muss warten bis ein Anwalt anwesend ist","Empfang durch GF oder PRRC, unverzügliche Bereitstellung aller Qualitätsaufzeichnungen, Begleitung durch PRRC/QMB während des gesamten Aufenthalts","Inspektion darf nur mit 48h Vorankündigung stattfinden","Nur der Lagerleiter darf mit Inspektoren sprechen"], correct:1, explanation:"Inspektionsprotokoll: (1) Empfang durch GF oder PRRC, (2) unverzügliche Bereitstellung aller Qualitätsaufzeichnungen in aktueller Fassung, (3) Begleitung durch PRRC oder QMB während des gesamten Aufenthalts, (4) CAPA für festgestellte Abweichungen." },
  { id:8, question:"Welche Unterlagen müssen bei einer Inspektion unverzüglich vorgelegt werden können?", options:["Nur die aktuellen Einkaufsrechnungen","Wareneingangsprotokolle, Schulungsnachweise und Reklamationslisten in aktueller Fassung","Ausschließlich die PRRC-Freigabeprotokolle","Nur Dokumente der letzten 30 Tage"], correct:1, explanation:"Bei Inspektionen müssen unverzüglich vorgelegt werden: Wareneingangsprotokolle, Schulungsnachweise und Reklamationslisten — alle in aktueller Fassung. Dies erfordert eine ständig aktuell gepflegte Dokumentation." },
  { id:9, question:"Was sind die zwei kritischen Risiken laut SOP-REG-05?", options:["Preisrisiko und Lieferrisiko","Fristversäumnis bei Sicherheitsmeldungen (→ Entzug der Vertriebsberechtigung) und widersprüchliche Aussagen gegenüber Behörden durch fehlende zentrale Kommunikationsleitung","Nur Dokumentationsmängel","Risiken beim Transport von Inspektionsunterlagen"], correct:1, explanation:"Zwei kritische Risiken: (1) Fristversäumnis bei Sicherheitsmeldungen — kann zum Entzug der Vertriebsberechtigung führen. (2) Widersprüchliche Aussagen gegenüber Behörden durch fehlende zentrale Kommunikationsleitung (deshalb alles über die PRRC)." },
  { id:10, question:"Was passiert nach einer Inspektion mit festgestellten Abweichungen?", options:["Abweichungen werden intern archiviert und beim nächsten Audit geklärt","Abweichungen werden im Rahmen des CAPA-Prozesses unmittelbar bearbeitet","Nur schwerwiegende Abweichungen werden bearbeitet","Der Hersteller ist für die Bearbeitung verantwortlich, nicht EMIG"], correct:1, explanation:"Festgestellte Abweichungen werden im Rahmen des Korrekturmaßnahmen-Prozesses (CAPA) unmittelbar nach der Inspektion bearbeitet. Die Nachbereitung ist Teil des offiziellen Inspektionsprotokolls und wird dokumentiert." }
];

<style>{`
  @import url('https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Outfit:wght@300;400;500;600;700&family=DM+Mono:wght@400;500&display=swap');

  /* ── Override Docusaurus article defaults ── */
  .sop-page h1, .sop-page h2, .sop-page h3,
  .sop-page p,  .sop-page ul, .sop-page li { all: revert; }
  .sop-page h1, .sop-page h2, .sop-page h3,
  .sop-page p,  .sop-page ul, .sop-page li { font-family: 'Outfit', sans-serif; }

  /* ── Make the doc container full width ── */
  .theme-doc-markdown { max-width: 100% !important; }
  article.sop-wrap     { max-width: 100% !important; padding: 0 !important; }

  /* ══════════════════════════════════════
     OUTER WRAPPER
  ══════════════════════════════════════ */
  .sop-page {
    font-family: 'Outfit', sans-serif;
    color: #18181f;
    width: 100%;
    --accent: #c8983a;
    --accent-lt: #e8d5a8;
    --dark: #0e0e14;
    --radius: 14px;
  }

  /* ══════════════════════════════════════
     HERO — light, editorial, not a dark box
  ══════════════════════════════════════ */
  .sop-hero {
    background: #f8f7f3;
    border: 1px solid #e8e4dc;
    border-radius: 20px;
    padding: clamp(1.5rem, 4vw, 2.5rem) clamp(1.5rem, 4vw, 2.75rem);
    margin-bottom: 2rem;
    display: grid;
    grid-template-columns: 1fr auto;
    gap: 2rem;
    align-items: center;
    position: relative;
    overflow: hidden;
  }

  .sop-hero::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, #c8983a08 0%, transparent 60%);
    pointer-events: none;
  }

  .sop-hero-eyebrow {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 0.75rem;
  }

  .sop-hero-eyebrow-line {
    width: 28px;
    height: 2px;
    background: var(--accent);
    border-radius: 99px;
    flex-shrink: 0;
  }

  .sop-hero-eyebrow-text {
    font-size: 0.64rem;
    font-weight: 700;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--accent);
  }

  .sop-hero-title {
    font-family: 'Bebas Neue', sans-serif;
    font-size: clamp(2rem, 5vw, 3.2rem);
    color: #0e0e14;
    margin: 0 0 0.6rem;
    letter-spacing: 0.05em;
    line-height: 1;
  }

  .sop-hero-desc {
    font-size: clamp(0.82rem, 1.4vw, 0.9rem);
    color: #6b7280;
    margin: 0;
    line-height: 1.65;
    max-width: 500px;
  }

  .sop-hero-meta {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
    margin-top: 1.25rem;
  }

  .sop-hero-meta-item {
    display: flex;
    align-items: center;
    gap: 6px;
    font-size: 0.72rem;
    color: #9ca3af;
    font-weight: 500;
  }

  .sop-hero-meta-dot {
    width: 5px;
    height: 5px;
    border-radius: 50%;
    background: var(--accent);
    flex-shrink: 0;
  }

  /* KPI block on right */
  .sop-hero-kpis {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
    align-items: flex-end;
    flex-shrink: 0;
  }

  .sop-hero-kpi { text-align: right; }

  .sop-hero-kpi-val {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 2.2rem;
    color: var(--accent);
    line-height: 1;
    letter-spacing: 0.04em;
  }

  .sop-hero-kpi-label {
    font-size: 0.58rem;
    color: #9ca3af;
    text-transform: uppercase;
    letter-spacing: 0.14em;
    margin-top: 1px;
  }

  .sop-hero-divider {
    width: 1px;
    height: 24px;
    background: #e2ded8;
    align-self: center;
  }

  /* ══════════════════════════════════════
     TWO-COLUMN LAYOUT
     Left: content (flexible)
     Right: sticky nav (260px)
  ══════════════════════════════════════ */
  .sop-layout {
    display: grid;
    grid-template-columns: 1fr 240px;
    gap: 2.5rem;
    align-items: start;
  }

  .sop-content { min-width: 0; }

  /* Sticky side nav */
  .sop-sidenav {
    position: sticky;
    top: 80px;
    background: #fff;
    border: 1px solid #ece9e3;
    border-radius: 16px;
    padding: 1.25rem;
    overflow: hidden;
  }

  .sop-sidenav-title {
    font-size: 0.62rem;
    font-weight: 700;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: #9ca3af;
    margin-bottom: 0.9rem;
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .sop-sidenav-title::after {
    content: '';
    flex: 1;
    height: 1px;
    background: #f0ede8;
  }

  .sop-nav-item {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 0.6rem 0.75rem;
    border-radius: 9px;
    text-decoration: none;
    margin-bottom: 3px;
    transition: all 0.18s;
    border: 1px solid transparent;
  }

  .sop-nav-item:hover {
    background: #f8f7f3;
    border-color: #e8e4dc;
    color: #111;
  }

  .sop-nav-item.active {
    background: #fff8ec;
    border-color: rgba(200,152,58,0.25);
    color: #92400e;
  }

  .sop-nav-num {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 1rem;
    color: #d1cec8;
    line-height: 1;
    flex-shrink: 0;
    width: 20px;
    transition: color 0.18s;
  }

  .sop-nav-item.active .sop-nav-num { color: var(--accent); }

  .sop-nav-label {
    font-size: 0.78rem;
    font-weight: 600;
    color: #374151;
    line-height: 1.2;
  }

  .sop-nav-item.active .sop-nav-label { color: #92400e; }

  /* Progress bar inside sidenav */
  .sop-nav-progress {
    margin-top: 1rem;
    padding-top: 1rem;
    border-top: 1px solid #f0ede8;
  }

  .sop-nav-progress-label {
    font-size: 0.62rem;
    color: #9ca3af;
    text-transform: uppercase;
    letter-spacing: 0.14em;
    margin-bottom: 6px;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .sop-nav-progress-pct {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 0.9rem;
    color: var(--accent);
  }

  .sop-nav-bar-bg {
    height: 3px;
    background: #f0ede8;
    border-radius: 99px;
    overflow: hidden;
  }

  .sop-nav-bar-fill {
    height: 100%;
    background: linear-gradient(90deg, var(--accent), var(--accent-lt));
    border-radius: 99px;
    transition: width 0.4s ease;
  }

  /* Quiz button in sidenav */
  .sop-nav-quiz-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    width: 100%;
    margin-top: 0.9rem;
    padding: 9px 14px;
    background: #0e0e14;
    color: var(--accent-lt);
    border: none;
    border-radius: 10px;
    font-family: 'Outfit', sans-serif;
    font-size: 0.78rem;
    font-weight: 600;
    cursor: pointer;
    text-decoration: none;
    transition: all 0.2s;
    letter-spacing: 0.04em;
  }

  .sop-nav-quiz-btn:hover {
    background: #1a1a26;
    color: #fff;
  }

  /* ══════════════════════════════════════
     SECTION
  ══════════════════════════════════════ */
  .sop-section {
    margin-bottom: 3rem;
    scroll-margin-top: 100px;
  }

  .sop-section-hd {
    display: flex;
    align-items: flex-start;
    gap: 14px;
    margin-bottom: 1.5rem;
    padding-bottom: 1rem;
    border-bottom: 1px solid #f0ede8;
  }

  .sop-section-num {
    font-family: 'Bebas Neue', sans-serif;
    font-size: clamp(2.2rem, 5vw, 3rem);
    color: #ede9e2;
    line-height: 1;
    flex-shrink: 0;
    letter-spacing: 0.04em;
  }

  .sop-section-title {
    font-size: clamp(1rem, 2.5vw, 1.2rem);
    font-weight: 700;
    color: #111827;
    margin: 0 0 3px;
    line-height: 1.2;
    padding-top: 0.3rem;
  }

  .sop-section-sub {
    font-size: 0.68rem;
    color: #9ca3af;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    font-weight: 600;
  }

  /* ══════════════════════════════════════
     INFO BOX
  ══════════════════════════════════════ */
  .sop-info {
    background: #fffbf3;
    border: 1px solid rgba(200,152,58,0.2);
    border-left: 3px solid var(--accent);
    border-radius: 12px;
    padding: 1rem 1.3rem;
    font-size: 0.87rem;
    color: #374151;
    line-height: 1.72;
    margin-bottom: 1.5rem;
  }
  .sop-info strong { color: #92400e; }

  /* ══════════════════════════════════════
     RULE CARDS
  ══════════════════════════════════════ */
  .sop-rules {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(min(100%, 260px), 1fr));
    gap: 11px;
    margin-bottom: 1.5rem;
  }

  .sop-rule {
    background: #fff;
    border: 1px solid #ece9e3;
    border-radius: var(--radius);
    padding: 1.2rem;
    display: flex;
    gap: 13px;
    align-items: flex-start;
    transition: border-color 0.18s, box-shadow 0.18s, transform 0.18s;
  }

  .sop-rule:hover {
    border-color: rgba(200,152,58,0.3);
    box-shadow: 0 4px 14px rgba(0,0,0,0.06);
    transform: translateY(-2px);
  }

  .sop-rule-icon {
    width: 36px; height: 36px;
    border-radius: 9px;
    display: flex; align-items: center; justify-content: center;
    font-size: 1.05rem;
    flex-shrink: 0;
  }

  .sop-rule-title {
    font-size: 0.86rem;
    font-weight: 700;
    color: #111827;
    margin-bottom: 4px;
    line-height: 1.3;
  }

  .sop-rule-desc {
    font-size: 0.77rem;
    color: #6b7280;
    line-height: 1.6;
    margin: 0;
  }

  .sop-rule-desc strong { color: #374151; }

  /* ══════════════════════════════════════
     PROCESS FLOW
  ══════════════════════════════════════ */
  .sop-flow { display: flex; flex-direction: column; margin-bottom: 1.5rem; }

  .sop-flow-step { display: flex; gap: 14px; align-items: flex-start; }

  .sop-flow-left {
    display: flex; flex-direction: column; align-items: center; flex-shrink: 0;
  }

  .sop-flow-circle {
    width: 32px; height: 32px; border-radius: 50%;
    background: #111116; color: #e8d5a8;
    font-family: 'Bebas Neue', sans-serif; font-size: 0.9rem;
    display: flex; align-items: center; justify-content: center;
  }

  .sop-flow-line {
    width: 1px; flex: 1; min-height: 20px;
    background: linear-gradient(to bottom, #dedad4, transparent);
    margin: 3px 0;
  }

  .sop-flow-content { padding-bottom: 1.4rem; flex: 1; }

  .sop-flow-title {
    font-size: 0.88rem; font-weight: 700; color: #111827; margin-bottom: 3px;
  }

  .sop-flow-desc {
    font-size: 0.79rem; color: #6b7280; line-height: 1.6; margin: 0;
  }

  /* ══════════════════════════════════════
     CHECKLIST
  ══════════════════════════════════════ */
  .sop-checklist { display: flex; flex-direction: column; gap: 7px; margin-bottom: 1.5rem; }

  .sop-check {
    display: flex; align-items: flex-start; gap: 11px;
    background: #fff; border: 1px solid #ece9e3; border-radius: 12px;
    padding: 0.9rem 1.1rem;
    transition: all 0.18s;
  }
  .sop-check:hover { border-color: rgba(200,152,58,0.25); background: #fffbf5; }

  .sop-check-icon {
    width: 28px; height: 28px; border-radius: 7px;
    background: #f5f3ee; display: flex; align-items: center;
    justify-content: center; font-size: 0.82rem; flex-shrink: 0;
  }

  .sop-check-label { font-size: 0.84rem; font-weight: 600; color: #111827; margin-bottom: 2px; }
  .sop-check-sub   { font-size: 0.73rem; color: #9ca3af; line-height: 1.45; }

  /* ══════════════════════════════════════
     WARNING / TIP
  ══════════════════════════════════════ */
  .sop-warning {
    background: #fef2f2; border: 1px solid #fecaca; border-radius: 12px;
    padding: 1rem 1.2rem; font-size: 0.83rem; color: #7f1d1d; line-height: 1.65;
    margin-bottom: 1.5rem; display: flex; gap: 10px; align-items: flex-start;
  }

  .sop-tip {
    background: #f0fdf4; border: 1px solid #bbf7d0; border-radius: 12px;
    padding: 1rem 1.2rem; font-size: 0.83rem; color: #14532d; line-height: 1.65;
    margin-bottom: 1.5rem; display: flex; gap: 10px; align-items: flex-start;
  }

  /* ══════════════════════════════════════
     KPI ROW
  ══════════════════════════════════════ */
  .sop-kpis {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(min(100%, 140px), 1fr));
    gap: 10px;
    margin-bottom: 1.5rem;
  }

  .sop-kpi {
    background: #fff; border: 1px solid #ece9e3;
    border-radius: var(--radius); padding: 1.1rem; text-align: center;
  }

  .sop-kpi-val {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 2rem; line-height: 1; letter-spacing: 0.04em; margin-bottom: 3px;
  }

  .sop-kpi-label {
    font-size: 0.64rem; color: #9ca3af; font-weight: 600;
    text-transform: uppercase; letter-spacing: 0.1em;
  }

  /* ══════════════════════════════════════
     NORM PILLS
  ══════════════════════════════════════ */
  .sop-norms { display: flex; flex-wrap: wrap; gap: 6px; margin-top: 1rem; }

  .sop-norm {
    background: #f5f3ee; color: #374151;
    font-size: 0.65rem; font-weight: 600; padding: 3px 9px;
    border-radius: 5px; font-family: 'DM Mono', monospace;
    letter-spacing: 0.04em; border: 1px solid #e8e4dc;
  }

  /* ── Divider ── */
  .sop-divider {
    height: 1px;
    background: linear-gradient(90deg, #e5e1da 0%, transparent 80%);
    margin: 2.5rem 0;
  }

  /* ══════════════════════════════════════
     QUIZ SECTION HEADER
  ══════════════════════════════════════ */
  .sop-quiz-header {
    background: #0e0e14;
    border-radius: 16px;
    padding: 1.5rem 2rem;
    margin-bottom: 1.5rem;
    display: flex;
    align-items: center;
    gap: 1.5rem;
  }

  .sop-quiz-header-icon {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 2.5rem;
    color: var(--accent);
    line-height: 1;
    flex-shrink: 0;
  }

  .sop-quiz-header-title {
    font-size: 1rem;
    font-weight: 700;
    color: #fff;
    margin-bottom: 3px;
  }

  .sop-quiz-header-sub {
    font-size: 0.75rem;
    color: rgba(255,255,255,0.38);
  }

  /* ══════════════════════════════════════
     RESPONSIVE
  ══════════════════════════════════════ */
  @media (max-width: 900px) {
    .sop-layout { grid-template-columns: 1fr; }
    .sop-sidenav { display: none; }
    .sop-hero  { grid-template-columns: 1fr; }
    .sop-hero-kpis { flex-direction: row; align-items: flex-start; justify-content: flex-start; }
    .sop-hero-kpi { text-align: left; }
  }

  @media (max-width: 640px) {
    .sop-rules { grid-template-columns: 1fr; }
    .sop-kpis  { grid-template-columns: repeat(2, 1fr); }
  }

  @media (min-width: 1400px) {
    .sop-layout { grid-template-columns: 1fr 260px; gap: 3rem; }
  }

  /* ── Fade in animation ── */
  @keyframes sop-in {
    from { opacity: 0; transform: translateY(6px); }
    to   { opacity: 1; transform: translateY(0); }
  }
  .sop-page { animation: sop-in 0.4s ease both; }
`}</style>

export function SopReg05Page() {
  const [activeSection, setActiveSection] = useState('zweck');
  const [scrollPct, setScrollPct] = useState(0);
  useEffect(() => {
    const ids = ['zweck','behoerden','benannte','inspektion','risiken','wissenstest'];
    const onScroll = () => {
      const el = document.querySelector('.sop-content');
      if (el) setScrollPct(Math.min(100, Math.round((Math.max(0,-el.getBoundingClientRect().top+100)/el.scrollHeight)*100)));
      let cur = 'zweck';
      ids.forEach(id => { const e = document.getElementById(id); if(e && e.getBoundingClientRect().top < 130) cur = id; });
      setActiveSection(cur);
    };
    window.addEventListener('scroll', onScroll, {passive:true});
    return () => window.removeEventListener('scroll', onScroll);
  }, []);
  const nav = [
    {id:'zweck',num:'01',label:'Zweck & Geltungsbereich'},
    {id:'behoerden',num:'02',label:'Behördenkommunikation'},
    {id:'benannte',num:'03',label:'Benannte Stellen'},
    {id:'inspektion',num:'04',label:'Inspektionsprotokoll'},
    {id:'risiken',num:'05',label:'Schnittstellen & Risiken'},
    {id:'wissenstest',num:'★',label:'Wissenstest'},
  ];
  return (
    <div className="sop-page">
      <div className="sop-hero">
        <div>
          <div className="sop-hero-eyebrow"><div className="sop-hero-eyebrow-line"/><span className="sop-hero-eyebrow-text">SOP-REG-05 · Regulatorik & MDR</span></div>
          <div className="sop-hero-title">Benannte Stellen<br/>&amp; Behörden</div>
          <p className="sop-hero-desc">Verantwortlichkeiten und Abläufe für die Kommunikation mit nationalen und internationalen Behörden sowie Benannten Stellen — zentralisiert über die PRRC, gesetzeskonforme Fristen und Inspektionsvorbereitung.</p>
          <div className="sop-hero-meta">
            <div className="sop-hero-meta-item"><div className="sop-hero-meta-dot"/>BfArM · MPDG · MDR</div>
            <div className="sop-hero-meta-item"><div className="sop-hero-meta-dot"/>Gültig ab 31.01.2026</div>
            <div className="sop-hero-meta-item"><div className="sop-hero-meta-dot"/>5 Kapitel · 10 Fragen</div>
          </div>
        </div>
        <div className="sop-hero-kpis">
          <div className="sop-hero-kpi"><div className="sop-hero-kpi-val">PRRC</div><div className="sop-hero-kpi-label">Zentrale Stelle</div></div>
          <div className="sop-hero-divider"/>
          <div className="sop-hero-kpi"><div className="sop-hero-kpi-val">0h</div><div className="sop-hero-kpi-label">Wartezeit</div></div>
          <div className="sop-hero-divider"/>
          <div className="sop-hero-kpi"><div className="sop-hero-kpi-val">CAPA</div><div className="sop-hero-kpi-label">Nachbereitung</div></div>
        </div>
      </div>

      <div className="sop-layout">
        <div className="sop-content">

          {/* 01 ZWECK */}
          <div className="sop-section" id="zweck">
            <div className="sop-section-hd"><div className="sop-section-num">01</div><div><div className="sop-section-title">Zweck & Geltungsbereich</div><div className="sop-section-sub">BfArM · Landesbehörden · Benannte Stellen · MiaMed-Gruppe</div></div></div>
            <div className="sop-info">Legt die Verantwortlichkeiten und Abläufe für die Kommunikation mit <strong>nationalen und internationalen Behörden</strong> sowie Benannten Stellen fest. Gilt für alle regulatorischen Interaktionen der EMIG GmbH — BfArM, Landesbehörden und Benannte Stellen der MiaMed-Hersteller.</div>
            <div className="sop-kpis">
              <div className="sop-kpi"><div className="sop-kpi-val" style={{color:'#c8983a'}}>PRRC</div><div className="sop-kpi-label">Zentralisiert</div></div>
              <div className="sop-kpi"><div className="sop-kpi-val" style={{color:'#ef4444'}}>0h</div><div className="sop-kpi-label">Verzögerung</div></div>
              <div className="sop-kpi"><div className="sop-kpi-val" style={{color:'#8b5cf6'}}>CAPA</div><div className="sop-kpi-label">Abweichungen</div></div>
              <div className="sop-kpi"><div className="sop-kpi-val" style={{color:'#3b82f6'}}>BfArM</div><div className="sop-kpi-label">Bundesbehörde</div></div>
            </div>

            {/* Verantwortungsmatrix */}
            <div className="sop-matrix" style={{background:'#fff',border:'1px solid #ece9e3',borderRadius:'14px',overflow:'hidden',marginBottom:'1.5rem'}}>
              <div style={{background:'#f8f7f3',padding:'0.75rem 1.1rem',fontSize:'0.65rem',fontWeight:700,textTransform:'uppercase',letterSpacing:'0.14em',color:'#9ca3af',borderBottom:'1px solid #ece9e3',display:'grid',gridTemplateColumns:'1fr 2fr',gap:'1rem'}}>
                <div>Funktion</div><div>Verantwortung</div>
              </div>
              {[
                {role:'Geschäftsführung',resp:'Bereitstellung der Infrastruktur bei Audits; abschließende Kommunikation in rechtlichen Grundsatzfragen',color:'#fef9c3',text:'#92400e'},
                {role:'PRRC',resp:'Koordiniert alle fachspezifischen Behördenanfragen; zentrale Anlaufstelle für regulatorische Meldungen',color:'rgba(200,152,58,0.1)',text:'#92400e'},
                {role:'QMB',resp:'Unterstützt bei der Vorbereitung der Qualitätsaufzeichnungen für externe Prüfungen',color:'#f5f3ee',text:'#374151'},
              ].map((r,i) => (
                <div key={i} style={{display:'grid',gridTemplateColumns:'1fr 2fr',gap:'1rem',padding:'0.85rem 1.1rem',borderBottom:i<2?'1px solid #f0ede8':'none',alignItems:'start'}}>
                  <div style={{fontSize:'0.78rem',fontWeight:700,color:r.text,background:r.color,padding:'3px 9px',borderRadius:'6px',display:'inline-block',whiteSpace:'nowrap'}}>{r.role}</div>
                  <div style={{fontSize:'0.78rem',color:'#374151',lineHeight:1.5}}>{r.resp}</div>
                </div>
              ))}
            </div>

            <div className="sop-norms"><span className="sop-norm">MDR 2017/745</span><span className="sop-norm">MPDG</span><span className="sop-norm">SOP-REG-03 PRRC</span></div>
          </div>

          <div className="sop-divider"/>

          {/* 02 BEHÖRDENKOMMUNIKATION */}
          <div className="sop-section" id="behoerden">
            <div className="sop-section-hd"><div className="sop-section-num">02</div><div><div className="sop-section-title">Kommunikation mit Behörden</div><div className="sop-section-sub">Zentralisiert über PRRC · Meldewesen · Amtliche Anfragen · Fristen</div></div></div>
            <div className="sop-info">Sämtliche Kommunikation mit Überwachungsbehörden wird <strong>über die PRRC zentralisiert</strong>. Kein anderer Mitarbeiter darf eigenständig behördliche Anfragen beantworten oder Meldungen absetzen.</div>
            <div className="sop-rules">
              {[
                {icon:'🚨',bg:'#fef2f2',t:'Meldewesen — schwerwiegende Vorkommnisse',d:'Meldung erfolgt elektronisch über EUDAMED/BfArM-Portale innerhalb der gesetzlichen Fristen. Keine Verzögerung aus wirtschaftlichen Gründen.'},
                {icon:'📧',bg:'#eff6ff',t:'Amtliche Anfragen vollständig beantworten',d:'Schriftliche Anfragen von Behörden sind innerhalb der gesetzten Fristen vollständig zu beantworten. PRRC stellt alle geforderten Unterlagen und Proben bereit.'},
                {icon:'📂',bg:'#f0fdf4',t:'Technische Dokumentation auf Anfrage',d:'Bei Behördenanfragen stellt EMIG die technische Dokumentation des Herstellers zur Verfügung — enge Abstimmung mit dem Hersteller erforderlich.'},
                {icon:'🔒',bg:'#f5f3ff',t:'Nur PRRC kommuniziert nach außen',d:'Kein anderer Mitarbeiter darf behördliche Fragen direkt beantworten. Bei Vor-Ort-Inspektion: alle Fragen werden an PRRC/GF weitergeleitet.'},
              ].map((r,i) => (
                <div className="sop-rule" key={i}><div className="sop-rule-icon" style={{background:r.bg}}>{r.icon}</div><div><div className="sop-rule-title">{r.t}</div><p className="sop-rule-desc">{r.d}</p></div></div>
              ))}
            </div>
            <div className="sop-warning"><span style={{fontSize:'1rem',flexShrink:0}}>⚠</span><div><strong>Fristversäumnis = Vertriebsentzug:</strong> Verspätete oder unvollständige Meldungen an Behörden können zum Entzug der Vertriebsberechtigung führen. Die PRRC überwacht aktiv alle Meldefristen.</div></div>
            <div className="sop-norms"><span className="sop-norm">MDR Art. 87 Vigilanz</span><span className="sop-norm">MPDG §86</span><span className="sop-norm">EUDAMED Portal</span></div>
          </div>

          <div className="sop-divider"/>

          {/* 03 BENANNTE STELLEN */}
          <div className="sop-section" id="benannte">
            <div className="sop-section-hd"><div className="sop-section-num">03</div><div><div className="sop-section-title">Interaktion mit Benannten Stellen</div><div className="sop-section-sub">CE-Zertifikatsprüfung · Auskunftspflicht · Herstelleraudits</div></div></div>
            <div className="sop-info">EMIG ist als Importeur/Händler meist <strong>nicht der direkte Vertragspartner</strong> der Benannten Stelle. Dennoch finden regelmäßige Interaktionen statt — insbesondere bei Herstelleraudits und Zertifikatsprüfungen.</div>

            {/* Was ist eine Benannte Stelle */}
            <div style={{background:'#0e0e14',borderRadius:'14px',padding:'1.4rem 1.75rem',marginBottom:'1.5rem'}}>
              <div style={{fontSize:'0.68rem',fontWeight:700,color:'rgba(200,152,58,0.7)',textTransform:'uppercase',letterSpacing:'0.18em',marginBottom:'1rem'}}>Was ist eine Benannte Stelle?</div>
              <div style={{display:'grid',gridTemplateColumns:'repeat(auto-fill,minmax(min(100%,200px),1fr))',gap:'10px'}}>
                {[
                  {label:'Definition',val:'Staatlich benannte Organisation die Konformitätsbewertungen von Medizinprodukten prüft',color:'#c8983a'},
                  {label:'Aufgabe',val:'Prüft ob Produkte der Klassen IIa, IIb und III die MDR-Anforderungen erfüllen',color:'#6ea8c8'},
                  {label:'Ergebnis',val:'Stellt CE-Zertifikate aus — ohne gültiges Zertifikat kein CE-Zeichen',color:'#10b981'},
                  {label:'Beispiele',val:'TÜV SÜD, TÜV Rheinland, BSI, SGS, Dekra — EU-weit akkreditiert',color:'#8b5cf6'},
                ].map((item,i) => (
                  <div key={i} style={{background:'rgba(255,255,255,0.05)',borderRadius:'10px',padding:'0.85rem'}}>
                    <div style={{fontSize:'0.68rem',fontWeight:700,color:item.color,textTransform:'uppercase',letterSpacing:'0.1em',marginBottom:'3px'}}>{item.label}</div>
                    <div style={{fontSize:'0.75rem',color:'rgba(255,255,255,0.5)',lineHeight:1.5}}>{item.val}</div>
                  </div>
                ))}
              </div>
            </div>

            <div className="sop-checklist">
              {[
                {icon:'✅',t:'Regelmäßige CE-Zertifikatsprüfung',d:'PRRC prüft regelmäßig die Gültigkeit der CE-Zertifikate der Lieferanten. Ablauf- oder Rückzugsdatum im System hinterlegt — automatische Erinnerung 3 Monate vor Ablauf.'},
                {icon:'🤝',t:'Unterstützung bei Herstelleraudits',d:'Bei Audits des Herstellers durch dessen Benannte Stelle unterstützt EMIG durch Bereitstellung von Rückverfolgbarkeitsdaten und Vigilanzsystem-Informationen für den EU-Markt.'},
                {icon:'📊',t:'Marktdaten für Benannte Stellen',d:'Daten über Produktperformance im EU-Markt (Reklamationen, Vigilanzmeldungen, Servicedaten) werden auf Anfrage der Benannten Stelle über den Hersteller weitergeleitet.'},
              ].map((c,i) => (
                <div className="sop-check" key={i}><div className="sop-check-icon">{c.icon}</div><div><div className="sop-check-label">{c.t}</div><div className="sop-check-sub">{c.d}</div></div></div>
              ))}
            </div>
            <div className="sop-norms"><span className="sop-norm">MDR Art. 51-60 Benannte Stellen</span><span className="sop-norm">SOP-EINK-01 Lieferantenbewertung</span></div>
          </div>

          <div className="sop-divider"/>

          {/* 04 INSPEKTIONSPROTOKOLL */}
          <div className="sop-section" id="inspektion">
            <div className="sop-section-hd"><div className="sop-section-num">04</div><div><div className="sop-section-title">Inspektionsprotokoll</div><div className="sop-section-sub">Empfang · Bereitstellung · Begleitung · CAPA-Nachbereitung</div></div></div>
            <div className="sop-info">Bei <strong>angekündigten oder unangekündigten Inspektionen</strong> am Standort Reutlingen wird folgendes 4-stufige Protokoll angewendet. Keine Ausnahmen — auch unangekündigte Inspektionen folgen exakt diesem Ablauf.</div>

            <div className="sop-flow">
              {[
                {t:'Empfang des Inspektors',d:'Inspektor/Auditor wird von der Geschäftsführung oder der PRRC empfangen. Keine anderen Mitarbeiter sprechen den Inspektor an oder beantworten Fragen eigenständig.',color:'#c8983a'},
                {t:'Sofortige Dokumentenbereitstellung',d:'Alle relevanten Qualitätsaufzeichnungen müssen unverzüglich in aktueller Fassung vorgelegt werden: Wareneingangsprotokolle, Schulungsnachweise, Reklamationslisten, Sperrwareprototokolle.',color:'#3b82f6'},
                {t:'Durchgehende Begleitung',d:'Der Inspektor wird während des gesamten Aufenthalts durch die PRRC oder die QMB begleitet. Keine unbegleiteten Momente im Lager oder Büro. Alle Fragen über PRRC/QMB.',color:'#8b5cf6'},
                {t:'CAPA-Nachbereitung',d:'Festgestellte Abweichungen werden im CAPA-Prozess unmittelbar bearbeitet. Inspektionsbericht wird archiviert. Korrekturmaßnahmen mit Zeitplan und Verantwortlichen dokumentiert.',color:'#10b981'},
              ].map((s,i,arr) => (
                <div className="sop-flow-step" key={i}>
                  <div className="sop-flow-left">
                    <div className="sop-flow-circle" style={{background:s.color}}>{i+1}</div>
                    {i<arr.length-1&&<div className="sop-flow-line"/>}
                  </div>
                  <div className="sop-flow-content">
                    <div className="sop-flow-title">{s.t}</div>
                    <p className="sop-flow-desc">{s.d}</p>
                  </div>
                </div>
              ))}
            </div>

            {/* Bereit-Checkliste */}
            <div style={{background:'#fff',border:'1px solid #ece9e3',borderRadius:'14px',padding:'1.4rem',marginBottom:'1.5rem'}}>
              <div style={{fontSize:'0.72rem',fontWeight:700,color:'#9ca3af',textTransform:'uppercase',letterSpacing:'0.12em',marginBottom:'0.9rem'}}>Inspektionsbereitschaft — jederzeit verfügbar</div>
              <div style={{display:'grid',gridTemplateColumns:'repeat(auto-fill,minmax(min(100%,200px),1fr))',gap:'8px'}}>
                {[
                  'Wareneingangsprotokolle (aktuell)',
                  'Schulungsnachweise aller Mitarbeiter',
                  'Reklamationsliste mit Status',
                  'Sperrlager-Protokolle',
                  'PRRC-Freigabeprotokolle',
                  'Lieferantenbewertungen',
                  'EUDAMED-Auszüge (aktuell)',
                  'Korrespondenzakte Behörden',
                  'Vigilanz-Logbuch',
                  'Aktuelle SOPs (alle Versionen)',
                ].map((item,i) => (
                  <div key={i} style={{display:'flex',alignItems:'center',gap:'8px',background:'#f8f7f3',borderRadius:'8px',padding:'0.55rem 0.75rem',fontSize:'0.74rem',color:'#374151',fontWeight:500}}>
                    <span style={{color:'#10b981',fontWeight:700,flexShrink:0}}>✓</span>{item}
                  </div>
                ))}
              </div>
            </div>

            <div className="sop-tip"><span style={{fontSize:'1rem',flexShrink:0}}>💡</span><div><strong>Vorbereitung ist alles:</strong> Unangekündigte Inspektionen kommen ohne Vorwarnung. Alle Dokumente müssen immer aktuell und sofort abrufbar sein — nicht erst bei Inspektion aktualisieren.</div></div>
            <div className="sop-norms"><span className="sop-norm">MPDG §86 Inspektionen</span><span className="sop-norm">SOP-CAPA-01</span><span className="sop-norm">SOP-REG-03 PRRC</span></div>
          </div>

          <div className="sop-divider"/>

          {/* 05 SCHNITTSTELLEN & RISIKEN */}
          <div className="sop-section" id="risiken">
            <div className="sop-section-hd"><div className="sop-section-num">05</div><div><div className="sop-section-title">Schnittstellen & Risiken</div><div className="sop-section-sub">Hersteller · Fristversäumnis · Widersprüchliche Aussagen · Aufzeichnungen</div></div></div>
            <div className="sop-info">Zwei kritische Risiken gefährden die regulatorische Stellung der EMIG GmbH und müssen aktiv durch die <strong>PRRC als zentrale Stelle</strong> gemanagt werden.</div>
            <div className="sop-rules">
              {[
                {icon:'🏭',bg:'#eff6ff',t:'Schnittstelle Hersteller',d:'Enge Abstimmung damit bei Behördenanfragen schnell auf technische Dokumentation zugegriffen werden kann. EMIG ist Bindeglied zwischen Behörde und Hersteller.'},
                {icon:'⏰',bg:'#fef2f2',t:'Risiko: Fristversäumnis',d:'Versäumte Meldefristen bei Sicherheitsvorkommnissen können zum Entzug der Vertriebsberechtigung führen. PRRC überwacht alle Fristen aktiv und proaktiv.'},
                {icon:'💬',bg:'#fef9c3',t:'Risiko: Widersprüchliche Aussagen',d:'Fehlende zentrale Kommunikationsleitung führt zu widersprüchlichen Aussagen gegenüber Behörden — schwerwiegendes Compliance-Risiko. Deshalb: nur PRRC kommuniziert nach außen.'},
              ].map((r,i) => (
                <div className="sop-rule" key={i}><div className="sop-rule-icon" style={{background:r.bg}}>{r.icon}</div><div><div className="sop-rule-title">{r.t}</div><p className="sop-rule-desc">{r.d}</p></div></div>
              ))}
            </div>
            <div style={{background:'#f8f7f3',borderRadius:'12px',padding:'1.1rem 1.3rem',marginBottom:'1.5rem'}}>
              <div style={{fontSize:'0.72rem',fontWeight:700,color:'#9ca3af',textTransform:'uppercase',letterSpacing:'0.12em',marginBottom:'0.75rem'}}>Aufzeichnungspflichten</div>
              {[
                {t:'Korrespondenzakte Behörden',d:'Chronologische Sammlung aller Schreiben und Meldungen an Behörden — lückenlos, chronologisch, dauerhaft archiviert.'},
                {t:'Inspektionsberichte',d:'Ergebnisse und Protokolle aller behördlichen Überprüfungen inklusive festgestellter Abweichungen und eingeleiteter CAPA-Maßnahmen.'},
                {t:'Vigilanz-Logbuch',d:'Dokumentation aller gemeldeten Vorkommnisse mit Datum, Meldeweg, Frist und Status der Nachverfolgung.'},
              ].map((a,i) => (
                <div key={i} style={{display:'flex',gap:'10px',marginBottom:i<2?'0.6rem':0,alignItems:'flex-start'}}>
                  <span style={{color:'#c8983a',fontWeight:700,flexShrink:0,fontSize:'0.8rem'}}>›</span>
                  <div><span style={{fontSize:'0.82rem',fontWeight:700,color:'#374151'}}>{a.t}: </span><span style={{fontSize:'0.78rem',color:'#6b7280'}}>{a.d}</span></div>
                </div>
              ))}
            </div>
            <div className="sop-norms"><span className="sop-norm">SOP-QM-03 Vigilanz</span><span className="sop-norm">SOP-REG-03 PRRC</span><span className="sop-norm">MPDG</span></div>
          </div>

          <div className="sop-divider"/>
          <div id="wissenstest"><div className="sop-quiz-header"><div className="sop-quiz-header-icon">★</div><div><div className="sop-quiz-header-title">Wissenstest — SOP-REG-05</div><div className="sop-quiz-header-sub">10 Fragen · Benannte Stellen & Behörden</div></div></div></div>
        </div>

        <div className="sop-sidenav">
          <div className="sop-sidenav-title">Inhalt</div>
          {nav.map(({id,num,label}) => (
            <a key={id} href={`#${id}`} className={`sop-nav-item${activeSection===id?' active':''}`}>
              <div className="sop-nav-num">{num}</div><div className="sop-nav-label">{label}</div>
            </a>
          ))}
          <div className="sop-nav-progress">
            <div className="sop-nav-progress-label"><span>Fortschritt</span><span className="sop-nav-progress-pct">{scrollPct}%</span></div>
            <div className="sop-nav-bar-bg"><div className="sop-nav-bar-fill" style={{width:`${scrollPct}%`}}/></div>
          </div>
          <a href="#wissenstest" className="sop-nav-quiz-btn">Zum Wissenstest →</a>
        </div>
      </div>
    </div>
  );
}

<SopReg05Page/>
<SopQuiz refCode="SOP-REG-05" questions={quizData}/>